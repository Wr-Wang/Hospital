<Window x:Class="Hospital.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="登录"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        MinWidth="360"
        Background="#F8FAFC">
    <Border Padding="24" Background="White" CornerRadius="6" BorderBrush="#E5E7EB" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Text="用户登录" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16" />

            <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,12">
                <TextBlock Text="用户名" Margin="0,0,0,4" />
                <TextBox Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}" MinWidth="280" />
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,12">
                <TextBlock Text="密码" Margin="0,0,0,4" />
                <PasswordBox x:Name="PasswordBox"
                             PasswordChanged="PasswordBox_PasswordChanged"
                             MinWidth="280" />
            </StackPanel>

            <TextBlock Grid.Row="3"
                       Text="{Binding ErrorMessage}"
                       Foreground="#B91C1C"
                       TextWrapping="Wrap"
                       Margin="0,0,0,12" />

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="取消" Command="{Binding CancelCommand}" Width="88" Margin="0,0,8,0" />
                <Button Content="登录" Command="{Binding LoginCommand}" Width="88" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
