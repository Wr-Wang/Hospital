apiVersion: v1
kind: Service
metadata:
  name: hospital-api
  namespace: hospital
spec:
  selector:
    app: hospital-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
