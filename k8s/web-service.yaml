apiVersion: v1
kind: Service
metadata:
  name: hospital-web
  namespace: hospital
spec:
  selector:
    app: hospital-web
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP
